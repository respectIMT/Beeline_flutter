import 'package:flutter/material.dart';

class Text_Ommabop extends StatelessWidget {
  const Text_Ommabop({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container(
      width: double.maxFinite,
      child: Text(
        "Ommabop",
        style: TextStyle(fontSize: 18),
      ),
    );
  }
}
